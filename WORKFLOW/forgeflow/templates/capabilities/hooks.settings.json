{
  "_comment": "TEMPLATE (FRAMEWORK.md §C11.5). Hooks = deterministic automation at fixed lifecycle events. Claude: merge this hooks block into .claude/settings.json or ~/.claude/settings.json. Codex: place equivalent project hooks in .codex/hooks.json or inline hooks tables in .codex/config.toml after the project config is trusted. Events vary by harness; keep commands repo-root-safe and delete this _comment key after copying.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/guard-destructive.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npm run -s lint:fix"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/write-memory-fact.sh"
          }
        ]
      }
    ]
  }
}
