<!-- TEMPLATE — How to use:
  The shared canon (FRAMEWORK.md move Reference (A3), principle #4). The ONE place every reusable pattern, token, and
  decision is defined. Every item links here and never re-defines a pattern. Governance is append/refine-
  only (§C9): a later batch may add edge-cases but must not break earlier items. Goal: "a developer should
  be able to implement any pattern from this document alone." Delete this comment when done. -->

# <PROJECT_NAME> — Shared Canon

> Canonical registry of reusable patterns, tokens, and decisions. Append/refine-only — never regress.
> Every item links to entries here; nothing re-defines a pattern locally.

## Design tokens / constants

| Token | Value | Role |
|-------|-------|------|
| `<token>` | `<exact value>` | <where used> |

## Pattern: <PATTERN_NAME>
- **Purpose**: <what it does>
- **Spec**: <exact sizes / structure / states>
- **States**: <default, loading, error, … with each defined>
- **Accessibility**: <labels, focus, alternatives>
- **Used by**: <items that reference it>
- **Established in**: Batch <N> · **Refined in**: Batch <M> (<what changed>)

## Pattern: <PATTERN_NAME>
<!-- repeat per pattern -->

## To be established (placeholders)
- **<future pattern>** — fill only when a real instance exists (do not invent ahead of need).

## Conventions
- Naming: <convention>
- Status / decision language: <convention>
