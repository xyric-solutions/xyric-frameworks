<!-- TEMPLATE — Verification matrix. Copy into a project, replace placeholders, and keep it aligned with hard gates, source hierarchy, and lane rules. -->

# <PROJECT_NAME> — Verification Matrix

Tie-breaker source: <ACTIVE_SOURCE_THAT_WINS_WHEN_DOCS_DISAGREE>

Use this matrix before closeout. Deterministic gates run first; manual review records judgment and evidence. A failed stop condition blocks `READY` unless an owner-approved waiver is recorded.

| Change type | Deterministic gates | Manual review lens | Evidence to record | Stop conditions |
|-------------|---------------------|--------------------|--------------------|-----------------|
| Backend/API | `<lint command>`; `<typecheck command>`; `<unit/API test command>`; contract/schema checks; route/auth checks | API compatibility, error semantics, idempotency, observability, rollback | Commands, status, changed endpoints/schemas, test output summary, linked work item | Breaking contract without migration; missing auth/tenant checks; untested error path; no rollback for risky change |
| Frontend/UI | `<lint command>`; `<typecheck command>`; `<component/E2E command>`; accessibility checks; screenshot/visual checks where relevant | User workflow fit, responsive layout, accessibility, copy honesty, interaction states | Commands, status, screenshots or notes, tested viewports, linked work item | Overlap/broken layout; inaccessible critical flow; fake or dead control; missing loading/error/empty state |
| DB migration | `<migration lint/check command>`; apply/rollback on test DB; generated client/schema checks; data backfill dry run | Data integrity, reversibility, locking/runtime risk, compatibility during deploy | Migration name, apply/rollback result, affected tables, row-count/backfill evidence | Destructive migration without approved plan; rollback impossible and not waived; auth/provenance fields changed without owner review |
| Docs/framework | `node framework/verify/portability-check.mjs .`; link/path checks; placeholder scan; stale-reference search | Active vs historical truth, source hierarchy consistency, handoff usefulness, template/starter separation | Commands, changed docs, active/archive declaration, tie-breaker source | Historical plan treated as active truth; root `AGENTS.md`/`CLAUDE.md` disagree; starter contains placeholders; missing shipped workflow wrapper |
| AI/data provenance | `<provenance test command>`; fixture checks; data lineage checks; eval/regression checks where available | Source boundaries, citation sufficiency, generated-vs-human labeling, model/data drift | Dataset/source IDs, model/version, eval output, cited source list, human review notes | Uncited claim in governed output; training/test leakage; provenance boundary crossed; generated content mislabeled |
| Auth/security | `<security test command>`; dependency/secrets scan; permission tests; session/token checks | Least privilege, identity fields, tenant isolation, secret handling, auditability | Commands, tested roles, identity field touched, scan output summary, reviewer signoff | Secret exposure; bypassable authorization; tenant/user identity confusion; new privileged path without tests |
| Health/safety | `<safety test/eval command>`; policy fixtures; escalation/refusal checks; audit log checks | Harm prevention, clinical/safety boundaries, emergency handling, disclaimers, human escalation | Commands, scenario set, safety reviewer notes, policy source, unresolved risks | Unsafe advice path; missing escalation for high-risk scenario; safety policy contradiction; unresolved clinical/legal owner question |
